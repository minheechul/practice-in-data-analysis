# ============================================================
# [과목명] 데이터 분석 템플릿 스크립트
# 작성자:
# 날짜:

# ============================================================
# 1. 환경 준비 ---------------------------------------------------
# ============================================================
library(here)
library(tidyverse)
# library(skimr)
# library(gtsummary)
# 필요한 패키지를 여기 추가하세요

# ============================================================
# 2. 데이터 불러오기 ---------------------------------------------
# ============================================================
# here()를 사용해 절대경로 대신 프로젝트 상대경로를 쓰세요.
# df <- read_csv(here("data", "raw", "example.csv"))

# ============================================================
# 3. 데이터의 기본적 특성 파악 -------------------------------------
# ============================================================
# 관측치 수, 변수 유형, 결측치, 이상치 등을 먼저 확인하세요.
# glimpse(df)
# skimr::skim(df)
# summary(df)

# ============================================================
# 4. 데이터 변형 --------------------------------------------------
# ============================================================
# 변수 생성, 필터링, 표본 선정 기준 등.
# 표본을 왜 이렇게 선택했는지(예: 특정 연도만 사용, 결측 제거)는
# 주석으로 반드시 남기세요 — 보고서 "데이터" 절에 그대로 옮겨 쓰게 됩니다.

# df_clean <- df |>
#   filter(...) |>
#   mutate(...)

# ============================================================
# 5. 시각화 및 표 작성 ---------------------------------------------
# ============================================================
# 탐색적 시각화. 여기서 필요한 게 보이면 4번으로 돌아가 변수를
# 추가/수정하세요.

# ggplot(df_clean, aes(x = ..., y = ...)) +
#   geom_...()

# 기술통계표
# gtsummary::tbl_summary(df_clean)

# ============================================================
# 6. 모형 추정 ----------------------------------------------------
# ============================================================
# 0번에서 적은 연구 질문 유형에 맞는 모형을 선택하세요.

# fit <- lm(y ~ x, data = df_clean)
# summary(fit)

# modelsummary::modelsummary(fit)

# ============================================================
# 7. 결과 저장 (보고서에서 불러 쓸 것들) ----------------------------
# ============================================================
# 보고서(Word)에 붙여넣을 표/그림을 파일로 저장해두면 편합니다.

# ggsave(here("output", "fig1.png"), width = 6, height = 4)
# saveRDS(fit, here("output", "fit.rds"))